/**
 * Re-export of maskSecret from src/mitm/maskSecrets.ts.
 * Preserves the module name used in plano 12 (Traffic Inspector).
 * The single implementation lives in src/mitm/maskSecrets.ts (D8).
 */
export { maskSecret } from "@/mitm/maskSecrets";
