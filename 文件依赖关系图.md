# htmlstars 文件依赖关系图

> 用途：从任意一个文件出发，沿箭头即可找到与之关联的其他文件。
> 箭头含义：`A --> B` 表示 A 引用 / 加载 / 依赖 / 调用 B。
> 渲染方式：VS Code 安装 Mermaid 插件，或粘贴到 https://mermaid.live 。

```mermaid
graph LR
    %% ============ 入口与前端框架 ============
    index_html["index.html"] --> runtime_js["static/js/runtime.js"]
    index_html --> index_app_js["static/js/index_app.js"]
    index_html --> index_css["static/css/index.css"]

    %% ============ runtime.js：双模运行时（最先加载） ============
    runtime_js -->|拦截 fetch| detect["/api/health 探测模式"]
    runtime_js -->|拦截| auth_api["/auth/*"]
    runtime_js -->|拦截 POST| dxf_api["/dxf"]
    runtime_js -->|拦截| search_api["/search/api/search"]
    runtime_js --> pyodide_js["static/pyodide/pyodide.js"]
    runtime_js --> manifest["static/pyodide/packages/manifest.json"]
    runtime_js -->|boot 写入虚拟FS| pysub_init["pysub/__init__.py"]
    runtime_js -->|boot 写入虚拟FS| browser_server["pysub/browser_server.py"]
    runtime_js -->|boot 写入虚拟FS| dxf_gen["pysub/dxf_generator.py"]
    runtime_js -->|boot 写入虚拟FS| profile_eng["pysub/profile_engine.py"]
    runtime_js -->|boot 写入虚拟FS| users_json["users.json"]
    runtime_js -->|boot 写入虚拟FS| menus_json["menus.json"]
    runtime_js -->|按需拉取| excel_db["static/data/excel_search.db"]
    runtime_js -->|按需拉取| jieba_zip["static/pyodide/jieba.zip"]
    runtime_js -->|按需安装| wheels["static/pyodide/packages/*.whl / *.zip"]

    %% ============ index_app.js：菜单与页面路由 ============
    index_app_js -->|fetch| auth_user["/auth/user"]
    index_app_js -->|fetch| auth_menus["/auth/menus → menus.json"]
    index_app_js -->|fetch| auth_login["/auth/login"]
    index_app_js -->|fetch| auth_logout["/auth/logout"]
    index_app_js -->|渲染| navMenu["#navMenu 菜单"]
    navMenu -->|点击加载| iframe_pages["pages/*.html (iframe)"]
    index_app_js -->|无权限| placeholder["pages/placeholder.html"]
    index_app_js -->|外部链接| external["pages/external-hint.html"]

    %% ============ 业务页面 ============
    led_pages["pages/led-design*.html 等设计页"] -->|fetch /dxf/| dxf_api
    search_page["pages/search.html"] -->|fetch /search/api/search| search_api
    pages_202607["pages/202607/*.html"] -->|相对引用| static_rel["../../static/*"]
    pages_202607 -->|同目录| js_202607["pages/202607/*.js（如 202607.js）"]
    pages_202608["pages/202608/*.html"] -->|相对引用| static_rel
    pages_202608 -->|同目录| js_202608["pages/202608/202608.js + lonely-html.js"]

    %% ============ pysub 前端模块（运行于 Pyodide 虚拟FS） ============
    browser_server -->|from .dxf_generator| dxf_gen
    browser_server -->|import| sqlite3_pkg["sqlite3（zip 解压）"]
    browser_server -->|import| jieba_pkg["jieba（zip）"]
    dxf_gen -->|from .profile_engine| profile_eng
    dxf_gen -->|import| ezdxf["ezdxf（wheel）"]
    dxf_gen -->|import| qrcode["qrcode（wheel）"]
    dxf_gen -->|import| barcode["python_barcode（wheel）"]
    profile_eng -->|函数内 import| ezdxf

    %% ============ 离线包依赖链 ============
    ezdxf -->|依赖| numpy["numpy（whl, loadPackage 注册 .so）"]
    ezdxf -->|依赖| fonttools["fonttools（whl）"]
    ezdxf -->|依赖| pyparsing["pyparsing（whl）"]
    ezdxf -->|依赖| typing_ext["typing_extensions（whl）"]

    %% ============ 后端 Flask 模式（app.pyw） ============
    app_pyw["app.pyw（Flask 后端）"] -->|register_blueprint| dxf_bp["pysub/dxf.py（bp /dxf）"]
    app_pyw -->|register_blueprint| search_bp["pysub/search.py（bp /search）"]
    app_pyw -->|register_blueprint| auth_bp["pysub/auth.py（bp /auth）"]
    app_pyw -->|import| utils["pysub/utils.py"]
    dxf_bp -->|动态 import| dxf_gen
    dxf_bp -->|from .utils| utils
    search_bp -->|import| sqlite3_pkg
    search_bp -->|import| jieba_pkg
    app_pyw -->|路由| static_route["/static/&lt;path&gt; → static/"]
    app_pyw -->|路由| pages_route["/pages/&lt;path&gt; → pages/"]

    %% ============ 文档 ============
    doc_txt["pysub/前端模式-预处理与扩展说明.txt"] -. 参考 .-> runtime_js
    struct_txt["项目结构.txt / README.md"] -. 说明 .-> index_html
```

## 双模对应关系（最容易看串的地方）

| 功能 | 前端 Pyodide 模式 | 后端 Flask 模式 |
|------|------------------|------------------|
| 模式探测 | `runtime.js` 拦截 `/api/health` 得 404 → frontend | `app.pyw` 提供 `/api/health` 200 → backend |
| 绘图 /dxf | `pysub/browser_server.py` 在 Pyodide 内 `runPython` 执行 | `pysub/dxf.py` (Blueprint) |
| 搜索 /search | 同上 `browser_server.search_api` + sqlite3/jieba | `pysub/search.py` (Blueprint) |
| 登录 /auth | `runtime.js` fallback 到内置 `users.json`/`menus.json` | `pysub/auth.py` (Blueprint) |
| 重型依赖 | 离线包（numpy/ezdxf/sqlite3/jieba） | pip 环境（`加载pip环境.pyw`）|

## 快速索引（从关键文件顺藤摸瓜）

- **改了 `runtime.js`** → 必须同步升 `RUNTIME_VERSION` 并改 8 处 HTML 的 `?v=` 强刷；它决定走哪套模式、加载哪些离线包。
- **绘图报错** → 查 `dxf_api` → `browser_server.dxf_generate` → `dxf_generator.py` → 离线包清单 `manifest.json`。
- **搜索报错** → 查 `search_api` → `browser_server.search_api` → `sqlite3`/`jieba` 离线包 + `excel_search.db`。
- **页面打不开 / 0 应用** → 查该页 `static/` 引用是否为相对路径（部署在子路径 `/v1/` 时空 `/static/` 会 404）。
- **本地后端起不来** → 查 `app.pyw` → 各 Blueprint → `pysub/utils.py`。
